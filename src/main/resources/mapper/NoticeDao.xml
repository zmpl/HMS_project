<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.NoticeDao">

    <resultMap type="com.entity.Notice" id="NoticeMap">
        <result property="noticeid" column="noticeId" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createtime" column="createTime" jdbcType="OTHER"/>
        <result property="userid" column="userId" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="NoticeMap">
        select
          noticeId, title, content, createTime, userId
        from pms.notice
        where noticeId = #{noticeid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="NoticeMap">
        select
          noticeId, title, content, createTime, userId
        from pms.notice
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="NoticeMap">
        select
          noticeId, title, content, createTime, userId
        from pms.notice
        <where>
            <if test="noticeid != null">
                and noticeId = #{noticeid}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createtime != null">
                and createTime = #{createtime}
            </if>
            <if test="userid != null">
                and userId = #{userid}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="noticeid" useGeneratedKeys="true">
        insert into pms.notice(title, content, createTime, userId)
        values (#{title}, #{content}, #{createtime}, #{userid})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update pms.notice
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createtime != null">
                createTime = #{createtime},
            </if>
            <if test="userid != null">
                userId = #{userid},
            </if>
        </set>
        where noticeId = #{noticeid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from pms.notice where noticeId = #{noticeid}
    </delete>

</mapper>